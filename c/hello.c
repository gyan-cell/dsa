#include <stdio.h>
int sampleFunction(int a, int b) { return a + b; }

int main(void) {
  printf("Hello World\n");
  int answer = sampleFunction(10, 20);
  printf("The answer is %d\n", answer);
  return 0;
}

// c is a programming language
